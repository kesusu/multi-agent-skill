# 版本演进记录

从 v1 到 v12.5，18 个版本的演进路径。核心改进方向从"加功能"转向"做减法+硬化执行约束"。

## v1 — 初版（过度膨胀）

**内容**：5条原则 + 角色池 + 固定流程模板 + 反模式

**发生了什么**：第一版试图把所有能想到的机制都塞进去。角色从模板选、流程写死、规则堆叠。写完发现角色之间职责重叠、流程步骤自相矛盾。

**教训**：做减法比加法重要。加规则容易，让规则不互相矛盾难。

## v2 — 整合外部指导

**改动**：加系统边界 + 用户交互协议 + 调度头

**为什么**：试图从外部指导文档中吸收最佳实践。结果是又加了一层复杂度。

## v3 — 反思内化

**改动**：动态角色 + 依赖分析 + 全局审视

**转折点**：开始从"固定模板"转向"动态生成"。这是第一个真正有价值的改进——角色不再从预设列表选，而是根据任务特征现场生成。

## v4 — 精简（关键版本）

**改动**：合并冗余、删除固定模板、统一表述（334→208行）

**发生了什么**：v1-v3 累积的冗余到了自相矛盾的程度。v4 大刀阔斧砍掉了近 40% 的内容，剩下的才是核心。

**教训**：框架膨胀到自相矛盾时，精简比继续加更有效。

## v5 — 补回关键内容

**改动**：补回硬约束 + 复盘格式 + 任务类型映射 + 调度员执行者例外（208→240行）

**为什么**：v4 砍过头了，一些必要的约束被删掉。v5 择优补回。

## v6 — 僵化修正

**改动**：信号驱动审视 + 字数改为建议 + 低风险角色自批 + 版本编号区分简单/复杂

**关键改进**：引入"信号驱动"概念——不是每 2 轮机械停下来审视，而是出现特定信号时才暂停。

## v7 — 整合自进化

**改动**：结构化自评估 + 用户反馈 + 经验沉淀 + 防退化规则（240→250行）

**关键改进**：首次加入"经验沉淀"机制——复盘纪要中的改进建议追加到 memory。但此时还是建议性的，没有强制执行。

## v8 — 对齐 CLAUDE.md

**改动**：加先读现场 + 交付前验证 + 根因分析 + 经验闭环机制

**关键改进**：加入"先读现场再拆解"原则——不读上下文就派 Agent = 拿到任务就派 Agent。

## v9 — 防过度工程化（关键版本）

**改动**：加前置分流器（3个yes/no问题）→ 极简/框架/探针三种模式 + 探针 Agent + 极简模式禁读 memory/派 Agent/写复盘 + 反模式扩至14条（308行）

**为什么重要**：解决了"简单任务也走完整流程"的浪费问题。分流器是整个方法论中最有实际价值的设计之一。

**教训**：简单问题复杂化是最大陷阱。

## v9.1 — 角色命名修正

**改动**：角色名称必须贴合任务上下文，模板是职责库不是命名池

**触发事件**：首次测试发现用了模板名"批评家"而非贴切的"代码审查员"。

## v10 — 实战经验回写

**改动**：第1步读经验加"转化为决策检查点"要求 + 5条新经验 + 2条新质量红线

**关键改进**：经验不再只是"记录"，而是必须转化为具体的检查动作。解决"经验写了不用等于没写"的问题。

## v11 — 反思闭环修复

**改动**：第1步读经验加具体转化格式 + 第5步加强制反思沉淀清单（4项 checklist）+ 反模式新增第15条

**触发事件**：用户审计发现"经验沉淀"和"经验闭环"都是描述性建议，没有接入执行流程。v11 把反思沉淀硬化为强制 checklist（不做 = 任务未完成）。

## v11.1 — 服务对象视角内置

**改动**：验收者角色定义加"必须代入服务对象视角" + 验收闭环 checklist 加第5项

**触发事件**：用户指出"写入 memory 换文件夹就没了"，应进 skill 主文件。且不是加新角色，而是验收者本身的职责扩展。

## v12 — 审计空壳（关键转折）

**改动**：首次用多 Agent 框架审查 skill 自身的规则执行情况。

**发生了什么**：审计发现 v12 的全部规则在实际任务中没有被执行——规则写得完整，但调度员在实战中没有走检查流程。规则是"装饰品"而非"强制机制"。

**教训**：写规则容易，让规则被执行难。机制的价值不在于设计得多好，而在于执行时能不能被违反。如果违反成本低于遵守成本，机制就是装饰品。

## v12.1 — 自我检查机制

**改动**：加入调度员自我检查——每轮调度后自检是否遗漏了强制流程。

**触发事件**：v12 审计暴露的问题修复第一步：从"写了就完事"到"写完要自检"。

## v12.2 — 元检查机制

**改动**：加元检查层——检查"自我检查是否被跳过"。

**为什么**：v12.1 的自检本身也可能被跳过。需要检查检查者的机制。

## v12.3 — 审计员双层机制

**改动**：引入独立审计员角色，与调度员的自检/元检查形成双层验证。

**关键改进**：外部审计比内部自检更可靠。调度员自己看不到自己的盲区。

## v12.4 — 措辞降级与定位修正

**改动**：批评家审查后修正 15 项问题，核心变化：
- "创新机制"降级为"核心机制设计"
- 竞品对比加置信度标注（[已验证]/[待确认]/[推测]）
- 开头加定位声明（".md 文件，不是框架"）

**触发事件**：批评家指出过度包装问题——范畴错误（.md vs Python 框架）、至少 4 个"独特"机制竞品也有。

## v12.5 — 冻结版本

**改动**：加入 3 个 Swarms 改进（depends_on 依赖声明、Wave 批次调度、计划审查机制），反模式从 16 条增至 19 条。Skill 主文件冻结，不再修改，直到验证数据出来。

**触发事件**：用户喊停"不再改了"。12 个版本的教训：继续加规则会分散注意力，v12 全部规则空壳就是证据。先验证，再迭代。

---

## 演进模式总结

| 阶段 | 版本 | 主旋律 |
|---|---|---|
| 膨胀期 | v1-v3 | 加功能、加规则，复杂度上升 |
| 精简期 | v4-v5 | 砍冗余、补必要，找到平衡 |
| 硬化期 | v6-v9 | 从建议变为规则，从规则变为机制 |
| 闭环期 | v10-v11.1 | 经验沉淀从"做了好"变为"不做不行" |
| 审计期 | v12-v12.4 | 发现规则没被执行，硬化为强制检查机制 |
| 冻结期 | v12.5 | 停止迭代，等待验证数据 |
